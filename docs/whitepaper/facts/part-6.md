# Part 6 — Phase 0 measurements, the range_cardinality table, permutation cost, scoping, open questions, leak register

## Measurement conditions (carry with every number)

- Environment for all Phase 0 measurements: WSL2, 12 cores, 39 GB, RTX 3080 — source: probes/results.md §(header) — quote: "Environment throughout: WSL2, 12 cores, 39 GB, RTX 3080. **Timings are indicative, not certified** — WSL2 was chosen for mmap and page-cache fidelity, not latency. Treat ratios as evidence and absolutes as a starting point."
- Same environment restated in the memo — source: probes/phase0-memo.md §(header) — quote: "Environment: WSL2, 12 cores, 39 GB, RTX 3080; timings indicative, not certified."
- Corpus provenance: two real sources joined — arXiv topic-embeddings dataset and Kaggle arXiv metadata snapshot — source: probes/dataset.md §1 — quote: "**Geometry / text** | [AliMaatouk/arXiv-Topics-Embeddings](https://huggingface.co/datasets/AliMaatouk/arXiv-Topics-Embeddings) — 2,422,486 arXiv papers, one BGE-large-en-v1.5 topic embedding each (4.4 GB parquet) | **Metadata** | Kaggle `Cornell-University/arxiv` snapshot v296 — 3,113,330 records: id, categories, authors, versions (5.1 GB JSON, anonymous `kagglehub` download works)"
- Join verdict: clean, no normalisation needed — source: probes/dataset.md §1 — quote: "**Join probe verdict: clean.** All 2,422,486 embedding IDs match a snapshot `id` verbatim — no normalisation needed in either direction."
- Base corpus size: 2,422,486 rows, 117 MB — source: probes/dataset.md §2 — quote: "117 MB, 2,422,486 rows. Built once by `build_corpus.py`; **nothing downstream reads the raw sources**."
- Corpus scaled by treating entity-ID prefixes as whole coherent corpora at four scales (250,000 / 2,422,486 / 250,000,000 / 1,000,000,000), with 250M and 1B built by adding transformed replicas of the real data, not by duplicating it — source: probes/dataset.md §4.1 — quote: "Entity IDs are append-only, so **a prefix of entity space is a whole, coherent corpus**... | 250,000,000 | 250,000,000 | + transformed replicas | | 1,000,000,000 | 1,000,000,000 | + transformed replicas — the design's target |"
- Replicas are affine transforms plus jitter, not identical copies, specifically to avoid degenerate mask/spatial structure — source: probes/dataset.md §4.3 — quote: "Naive tiling would make the corpus **easier** than the base: identical copies give every term N identical posting blocks and every tile the same occupancy, so mask structure and spatial structure both go degenerate. Each replica is instead an affine transform (rotate, reflect, log-uniform scale 0.02–0.55, translate) plus jitter, with 45% placed on shared hubs..."
- Policies used throughout Phase 0 are synthetic, generated to bracket distributional shapes, not real access-control labels — source: probes/phase0-memo.md §1 — quote: "A **rehearsal on a real corpus with synthetic policies**: 2.42M arXiv papers (metadata × topic embeddings; join verdict go, with the corpus caveats in §4), and label generators pinning the distributional knobs real policies would set — realistic skew (categories, surnames), orthogonal controls (hash), 361,837-term vocabulary, per-item noise."
- Restated directly in dataset.md's own scoping section — source: probes/dataset.md §7 — quote: "**Cannot**: say anything new about how *real* access-control labels cluster. Replicas repeat the base's topic geometry under transformation, so spatial-autocorrelation conclusions belong to the 2.4M real prefix (and, ultimately, to the real-label rerun). The label configs are synthetic policies chosen to bracket distributional shapes, not to predict any deployment's."
- Mask coverage at the true 10^9 (categories-subclass, realistic worst case) reaches up to 41.6% for random w=10,000 grants, and category-archive random reaches 72.0%; hash-flat random reaches 100.0% (degenerate) — source: probes/results.md §4.2 table (transcribed below).
- The one hashed (non-reproducible) artefact is the UMAP geometry, hash `4a59a9b8…` — source: probes/dataset.md §3 — quote: "**The one hashed artifact**, sha256 `4a59a9b8…`." and "cuML's `nn_descent` is **not bit-reproducible even under a fixed `random_state`** — which is exactly why geometry is hashed rather than seeded."
- Corpus representativeness caveat: embeddings coverage ends 2024-09, background 3–6%/year of snapshot papers lack embeddings — source: probes/phase0-memo.md §4 — quote: "Corpus representativeness: embeddings coverage ends 2024-09 and a background 3–6%/year of snapshot papers lack embeddings (join-probe findings), so the corpus is the joined set, not \"arXiv\"."
- The 103× tiling (scaling to 10^9 via replicas) is valid for build cost/size only, holds vocabulary fixed, and overstates union cost for author-like policies at real scale (conservative error) — source: probes/phase0-memo.md §4 — quote: "The 103× tiling is valid for build cost/size only and holds vocabulary fixed — for author-like policies a real 250M corpus would have a larger surname vocabulary and smaller per-term postings, so the measured union costs overstate that case (the error is conservative)."

## §6 of results.md — masked range_cardinality table (transcribed exactly)

Context/method — source: probes/results.md §6 — quote: "`range_cardinality` over real Morton tiles, min of 5 (a single-shot first pass reported figures 5–10× higher and was noise — repeat before believing). Mask: 6 global terms → 69.3M items, 6.93% coverage."

| Depth | Tiles | Rows/tile | Visible | Total | µs/tile |
|---|---|---|---|---|---|
| 0 | 1 | 1,000,000,000 | 69,329,737 | **0.19 ms** | 190.9 |
| 2 | 16 | 62,500,000 | 69,329,737 | **0.10 ms** | 6.3 |
| 4 | 256 | 3,906,250 | 69,329,737 | **0.31 ms** | 1.2 |
| 6 | 300 | 266,845 | 6,448,061 | 0.31 ms | 1.0 |
| 8 | 300 | 22,612 | 584,004 | 0.26 ms | 0.9 |
| 10 | 300 | 9,597 | 288,780 | 0.25 ms | 0.8 |
| 12 | 300 | 8,637 | 268,300 | 0.24 ms | 0.8 |

- Conclusion drawn immediately from the table — source: probes/results.md §6 — quote: "**Whole-viewport exact masked counting costs 0.1–0.3 ms at every zoom level**, including depth 0 where one call counts the entire billion rows in 191 µs. Cost is O(containers spanned) and the mask has only ~15,000 containers, so any set of ranges is bounded by one full traversal — it cannot blow up at coarse zoom. This **corrects a worry in §13.3**: the zoomed-out overview is expensive because of mask materialisation and shard fan-out, not because of counting."

## Permutation into row space — cost and caching rationale

- CLAIM — the permutation into row space, not the range_cardinality count, is the expensive step: 8.8 s for a 69M-item mask — source: probes/results.md §6 — quote: "The **permutation into row space is the expensive step** — 8.8 s for a 69M-item mask. That is exactly why §10.4 caches it per *(token, slice, pin)*, and the number makes the consequence concrete: if it ever drifts onto the per-viewport path, the system dies."
- CLAIM — this is why it is cached per (token, slice, pin) rather than recomputed per viewport — source: probes/results.md §6 (same quote as above).

## What Phase 0 established, and precisely what it did not

- CLAIM — overall verdict is GO, scoped, and the Phase 0 gate explicitly stays open pending a real-label rerun — source: probes/phase0-memo.md §(header) — quote: "**Date:** 2026-07-27. **Recommendation: GO — scoped.** Go for Phase 1 spend: nothing measured contradicts an invariant-bearing assumption, and several findings close escape hatches. **The plan's Phase 0 gate itself stays open**: plan §4 requires real predicates and real grant sets, this run used synthetic policies over a real corpus, and the real-label rerun (action 3) **retains full go / rework / stop authority** — it is not parameter tuning."
- CLAIM — established: the mask-build serving path (postings union) is the correct authorise-path formulation, and the semi-join is reassigned to build-cadence machinery / the differential oracle — source: probes/phase0-memo.md §2.2 — quote: "this **reassigns the semi-join** from the plan's \"authorise budget\" role to build-cadence machinery and the differential oracle."
- CLAIM — established: mask sizes match Appendix A's dense-bound arithmetic exactly (31.2 MB at 250M for ≥25% coverage) — source: probes/phase0-memo.md §2.2 — quote: "Mask sizes sit exactly on Appendix A's dense-bound arithmetic (31.2 MB at 250M for ≥25% coverage; the frozen-format sizes specifically are unmeasured — pyroaring exposes only the portable format — and fold into the croaring verification task)."
- CLAIM — established: realistic masks are essentially scattered under Morton order (flat-hash control 1.00, surnames 1.03–1.15, categories 1.7–2.32, archive head 5.11), and Morton order buys no Roaring compression for any family — source: probes/phase0-memo.md §2.2 — quote: "**Realistic masks are essentially scattered under Morton order**, and Morton buys no Roaring compression for any family."
- CLAIM — established: signature-sorted entity assignment (not created/arrival order) is what produces posting compression, and arrival order gives essentially none — source: probes/phase0-memo.md §3 finding 2 — quote: "**§11.1's posting compression is not free from arrival order** (run lengths 1.0–1.26 under created-order IDs) **but pays hard once implemented**: simulated signature-sorted assignment compresses postings 8.9× (categories) to 36.7× (t=100) and the 25%-coverage session fragment 6× to 1,614×..."
- CLAIM — established: I7's exact/direct-evaluation fallback is load-bearing, not a hypothetical simplification target — source: probes/phase0-memo.md §3 finding 3 — quote: "**I7's exact fallback is load-bearing**: for tail-only principals essentially every tile is below the crossover; the Phase 2 warning against \"simplifying\" it away is not hypothetical."
- CLAIM (explicit disclaimer) — Phase 0 did NOT measure DNF expansion, because the predicates are the project's own synthetic knob and measuring their expansion would be circular — source: probes/phase0-memo.md §1 — quote: "**DNF expansion was not measured** — our predicates are our own knob, so measuring their expansion is circular."
- CLAIM (explicit disclaimer) — the true terms/item distribution, and therefore the cap-adequacy kill criterion, remains unresolved and is assigned to the real-label rerun — source: probes/phase0-memo.md §1 — quote: "**The true terms/item distribution — and therefore the cap-adequacy kill criterion — remains the real-label rerun's**, since at t̄ ≥ 100 the cap is a policy parameter rather than a gate."
- CLAIM (explicit disclaimer) — synthetic policies bracket, and do not measure, real-label behaviour; this is the entire reason the go/rework/stop gate stays open — source: probes/phase0-memo.md §4 — quote: "Synthetic policies bracket, and do not measure, real-label behaviour — the gate in the first paragraph exists because of this."
- CLAIM (explicit disclaimer) — retrieve-path latency was deliberately left unmeasured in Phase 0, reserved for Phase 1's exit criterion on certified hardware — source: probes/phase0-memo.md §4 — quote: "retrieve-path latency deliberately unmeasured (Phase 1's exit criterion, on certified hardware)."
- CLAIM (explicit disclaimer, dataset.md) — the data can stress machinery (Morton sort at 10^9, tile tables, range arithmetic, u32 limits, hot cells, mask build across vocabulary/posting shapes, dictionary scale to 10^8 terms, per-item breadth to 1000) but cannot say anything new about how real access-control labels cluster — source: probes/dataset.md §7 — quote: "**Can**: stress the machinery... **Cannot**: say anything new about how *real* access-control labels cluster."
- CLAIM — nearly every Phase 0 headline (signature alignment, posting compression, union speed) is policy-dependent, not corpus-size dependent — the machinery is validated, the deployment's actual numbers are not — source: probes/results.md §8 — quote: "**Almost every headline is policy-dependent.** Signature alignment, posting compression and union speed all depend on the shape of the access labels, not on corpus size — the same histogram decides all three, and author-like policies get none of them while category-like policies get all three. This is the strongest argument for the real-label rerun retaining go/rework/stop authority: the *machinery* is validated, the *deployment's numbers* are not."
- CLAIM — design §16's residual autocorrelation question is closed by owner decision (r18): no real access-labelled corpus is available, so synthetic evidence is accepted as final, converting the open question into deployment guidance — source: .ignore/tessera-architecture-design.md §16 — quote: "The residual question — whether *real access labels* behave differently from these proxies — is **closed by owner decision (r18)**: no real access-labelled corpus is available to this project, so synthetic evidence is accepted as final, and the question converts to deployment guidance — any deployment with real labels re-runs the Phase 0 measurements before trusting the policy-dependent conclusions."

## Open questions — design §16 (as of r18/r17 revisions recorded there)

- CLAIM — term cardinality under the reference plugin is unresolved and gated on measuring real-predicate DNF expansion first — source: .ignore/tessera-architecture-design.md §16 — quote: "**Term cardinality under the reference plugin.** How many distinct terms exist, how many per item, and what fraction of items carry more than one? These size the index, the ingest cap and §7.9's histogram additivity. Under the reference plugin they depend on DNF expansion, which published measurement puts as infeasible beyond nesting depth 2 for comparable workloads — so **measuring the expansion factor on real predicates comes first**; it is where the design fails if it fails (Appendix E)."
- CLAIM — spatial clustering of a typical mask: measured (r16), closed by owner decision (r18) as above — source: .ignore/tessera-architecture-design.md §16 (quoted above, "How spatially clustered is a typical mask?").
- CLAIM — data separation vs audience separation: resolved (r17) as data separation — source: .ignore/tessera-architecture-design.md §16 — quote: "**Data separation or audience separation?** (§12.1.) **Resolved (r17):** data separation."
- CLAIM — open: how many compartments and how many combinations occur (sets fan-out bound and per-partition overhead budget) — source: .ignore/tessera-architecture-design.md §16 — quote: "**How many compartments, and how many combinations occur?** Sets the fan-out bound and the per-partition overhead budget (§12.4)."
- CLAIM — token lifetime: resolved (r17), default backstop one hour, deployment-overridable — source: .ignore/tessera-architecture-design.md §16 — quote: "**Token lifetime.** **Resolved (r17):** the default backstop is **one hour**, deployment-overridable; the caller's re-authorisation cadence remains the governing policy. At measured mask-build costs the extra authorise load is negligible, and the staleness bound tightens by an order of magnitude over the earlier 12-hour working assumption."
- CLAIM — which generating set the caller declares: resolved (r17) as the prompt sample (§7.8), recorded in manifest provenance — source: .ignore/tessera-architecture-design.md §16 — quote: "**Which generating set the caller declares** — **Resolved (r17):** the prompt sample (§7.8), recorded in manifest provenance."
- CLAIM — retroactive revocation across slices (§9): resolved (r17), no — current credentials govern all slices — source: .ignore/tessera-architecture-design.md §16 — quote: "**Retroactive revocation across slices (§9).** **Resolved (r17):** no — current credentials govern all slices; the shared-mask premise stands."
- CLAIM — open: sharded index placement (§13.3) — source: .ignore/tessera-architecture-design.md §16 — quote: "**Sharded index placement (§13.3).**"
- CLAIM — overflow item visibility (§6.2): resolved (r16) as moot — exclusion dropped, items always indexed, over-bound term sets warn — source: .ignore/tessera-architecture-design.md §16 — quote: "**Overflow item visibility (§6.2).** **Resolved (r16):** moot — exclusion is dropped; items are always indexed and over-bound term sets warn."
- CLAIM — open: entity ID exhaustion — consumed IDs exceed live items without bound; at 10^9, 2^32 headroom is thin; likely answer is shard-local u32 with a (partition, shard, offset) global ID — source: .ignore/tessera-architecture-design.md §16 — quote: "**Entity ID exhaustion.** Consumed IDs exceed live items without bound. At 10<sup>9</sup>, 2<sup>32</sup> headroom is thin, and columns, permutations and standard Roaring are all 32-bit. Shard-local u32 with a (partition, shard, offset) global ID is the likely answer."
- CLAIM — open: segment count tolerance — how many live segments before per-tile range fan-out is noticeable — source: .ignore/tessera-architecture-design.md §16 — quote: "**Segment count tolerance.** How many live segments before per-tile range fan-out is noticeable?"
- CLAIM — open: vector serving — whether source embeddings are served at view time, and expected selectivity of a typical filtered view (§8.3) — source: .ignore/tessera-architecture-design.md §16 — quote: "**Vector serving.** Are source embeddings served at view time, and what is the expected selectivity of a typical filtered view (§8.3)?"
- CLAIM — open: slice count — how many temporal slices must be simultaneously browsable — source: .ignore/tessera-architecture-design.md §16 — quote: "**Slice count.** How many temporal slices must be simultaneously browsable?"

## Appendix C — the complete leak register, C1–C16

| # | Channel | What leaks | Severity | Mitigation | Status |
|---|---|---|---|---|---|
| C1 | Node membership derives from global density | That the principal's visible items in a region group together — a fact about structure including unseen items | Low | `min_visible_members` against `M_auth` bounds how finely this is exposed; filtering cannot deepen it (**I12**) | Accepted |
| C2 | Node extent and hull shape | Recomputed per user from masked members only — no leak by construction; listed to record that it was checked | None | — | Closed |
| C3 | Label existence | A principal learns only of labels they satisfy | None | Response omits all unsatisfied candidates (§10.6) | Closed |
| C4 | Response timing | Viewport service time correlates weakly with unauthorised items scanned during candidate-list descent | Low | Unmitigated; quantify before treating as acceptable | Open |
| C5 | Extractive-tier background frequencies | Corpus-wide term distributions, if drawn from the live corpus | Low | Fixed public reference corpus (§7.7) | Closed |
| C6 | Entity ID gaps on the wire | Count, and under Morton assignment also location, of unauthorised items | Medium | **I10**; opaque per-session handles | Closed |
| C7 | Generating-set shrinking under deletion | A label reflecting content the principal may never have been entitled to | Medium | Not adopted; caller regenerates (§7.6) | Not adopted |
| C8 | Pre-intersection filter cardinality | A raw match count is a corpus-wide count over unauthorised records | High if exposed | Not exposed on un-intersected intermediates (§8.2) | Closed by construction |
| C9 | Text relevance scores and ranks | Corpus-global statistics allow inference of unreadable content | High if ranking added | Boolean filtering only (§8.3) | Closed by scope |
| C10 | Vector similarity results and thresholds | Post-filtered neighbours vary observably with invisible items | High if post-filtered | Threshold filters with candidate push-down (§8.2) | Closed by construction |
| C11 | Label filter vocabulary | Offering a filterable label reveals a label the principal cannot see | Medium | Vocabulary containment-filtered against `M_auth` (§8.3) | Closed |
| C12 | Caller-declared generating sets | A label supplied with an optimistic generating set is a disclosure the service will faithfully serve | High | Contract requirement (§2.4); provenance is unverifiable by the service | Accepted — caller's control |
| C13 | Cross-partition node metadata | A node whose members lie wholly inside a compartment reveals, by existing and having an extent, that something is there | Medium | Node metadata held per partition (§12.3); C1 crossing a physical boundary | Closed |
| C14 | Partition fan-out width | Query latency correlates with how many compartments a token reaches | Low | Unmitigated; the principal already knows their own clearances, so this reveals nothing about data | Accepted |
| C15 | Session pin rate of change | Corpus-wide ingest and compaction activity, visible in how often a viewer's pin identifier changes — activity, not content, and including activity on data the viewer cannot see | Low | Pin values are per-session scrambled so no cross-session correlation; the rate itself remains observable. C14-like in character | Accepted |
| C16 | Router-held label presence registry | That some label draws on a given compartment — held by the routing process, which already routes queries into that compartment | Low | Registry restricted to label IDs and required-set hashes: no text, no entity IDs, no cardinalities. Required for the I13 merge (a router ignorant of an unreachable slice serves labels it must withhold) | Accepted |

- CLAIM — ownership/review-date bookkeeping note attached to the register — source: .ignore/tessera-architecture-design.md Appendix C — quote: "Owner and review date for C1, C4, C12, C14, C15 and C16 to be assigned before launch."
- CLAIM — framing statement for the whole appendix — source: .ignore/tessera-architecture-design.md Appendix C — quote: "I2 requires displayed quantities to derive from visible data only. These are the known exceptions. Anything not listed is a bug, not a trade-off."

## Expected-but-absent

- The task asked to read design §13 and §16; §13 ("Scaling to 10^9 items") does not itself contain a leak-register-adjacent table or additional numeric results beyond what is transcribed above (§13.1 "what breaks," §13.2 "what does not break," §13.3 sharding trade, §13.4 staging) — these are prose statements, not measurements, and are captured qualitatively where relevant (sharding trade referencing r16/Phase 0 results) but are not independently numbered facts beyond what's quoted.
- No explicit hardware certification claim (e.g., a stated production/certified benchmark) exists anywhere in these sources — all sources repeatedly and explicitly disclaim that WSL2 timings are "indicative, not certified." Do not present any Phase 0 timing as production-certified.
- No number for "real-label" term cardinality, DNF expansion factor, or cap-adequacy kill-criterion result exists in these sources — the memo explicitly defers all three to the future real-label rerun (see phase0-memo.md §1 and action 3/4 quotes above). Do not supply a number for these.
