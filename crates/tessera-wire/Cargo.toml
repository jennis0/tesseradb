[package]
name = "tessera-wire"
version = "0.1.0"
edition = "2021"
license.workspace = true

[dependencies]
arrow = { workspace = true }
# zstd for a records frame's buffers, and only zstd: the reader a client registers is that one
# codec.
arrow-ipc = { version = "=59.1.0", default-features = false, features = ["zstd"] }
