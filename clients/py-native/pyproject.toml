[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[project]
name = "tesseradb-native"
version = "0.1.0"
description = "Tessera's platform wheel: the `tessera` executable and the `_tessera` extension module."
readme = "README.md"
license = "Apache-2.0"
requires-python = ">=3.10"
# Two compiled artifacts and an `__init__.py` that says where they are. Nothing to depend on.
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["tesseradb_native"]
# Both artifacts are produced by the hook and never committed, so they are declared here as well
# as force-included: hatchling would otherwise drop them as untracked.
artifacts = [
  "tesseradb_native/tessera",
  "tesseradb_native/tessera.exe",
  "tesseradb_native/_tessera.abi3.so",
  "tesseradb_native/_tessera.pyd",
]

[tool.hatch.build.targets.wheel.hooks.custom]
path = "hatch_build.py"

# An sdist carries no artifacts and cannot build them: the hook needs the Rust workspace, which an
# sdist does not contain. Build the wheel from the repository.
[tool.hatch.build.targets.sdist]
include = ["tesseradb_native", "hatch_build.py", "README.md"]

[tool.ruff]
line-length = 100

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F"]
