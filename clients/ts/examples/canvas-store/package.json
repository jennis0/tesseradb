{
  "name": "@tesseradb/example-canvas-store",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "description": "C2 with none of Tessera's rendering: the store under a hand-rolled camera on a 2D canvas, the host formatting the counts itself.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@tesseradb/client": "*"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vite": "^8.2.0",
    "vitest": "^3.2.0"
  }
}
