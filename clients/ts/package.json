{
  "name": "tessera-clients",
  "private": true,
  "workspaces": [
    "spike",
    "core",
    "deck",
    "components",
    "viewer",
    "harness",
    "wire-example"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present && tsc -p scripts/tsconfig.json"
  }
}
