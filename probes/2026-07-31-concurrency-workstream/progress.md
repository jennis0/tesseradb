# SDD ledger — plan: /home/joe/.claude/plans/i-d-like-you-to-jiggly-cupcake.md
Baseline: green at 6052af2 (cargo test --workspace, pipefail-verified). Env note: tessera-store test write_permutation_rejects_entity_id_not_fitting_u32 is OOM-SIGKILLed standalone under memory pressure — reproduces at base commit, pre-existing/environmental.
Task 1: minor (deferred): warm-hit key.clone() in single_flight get_or_build — clone only on miss (single_flight.rs:81)
Task 1: minor (deferred): single_flight unit tests hang-not-fail on regression — use recv_timeout (single_flight.rs:153,194)
Task 1: minor (deferred): overlap timing test headroom on low-core runners — gate on cores>=4 or scale ratio (tests/viewport.rs:~2009)
Task 1: minor (deferred): TDD RED was compile-failure only for two of three tests
Task 1: minor (deferred): get_or_build FnOnce()->V cannot express fallible build — doc line or Result signature (D-G wedge note)
Task 1: minor (deferred): stale #[allow(clippy::type_complexity)] on simplified cache type (session.rs:~240)
Task 1: cross-task note: bench Arm B (shared session, c=1000) opening burst now returns fail-closed 500s until Task 4 maps ProjectionBuilding->429; Task 9 accounting must expect 429s post-Task-4.
Task 1: note: Task 4 owner-ruling on contracts §3.1 429 annotation = user's plan approval (plan Task 4 pre-condition); spec amendment lands with Task 4.
Task 1: complete (commits 6052af2..d9baada, review clean — 6 minors deferred)
Task 2: important (deferred, ruling): unbounded in-memory fragment slot map pins mmaps for process lifetime (fragment.rs slots field) — plan's Task 1 text explicitly scopes cache eviction out of the workstream ("memory, not concurrency"); same ruling applied here for consistency; NEW in kind for fragments (memory was previously bounded by live sessions) — MUST be surfaced in final summary + final review triage as a workstream follow-up (bound/LRU/clear policy needs an argued knob, not an ad-hoc number).
Task 2: minor (deferred): Drop guard `self.slots.lock().unwrap()` can double-panic abort if map mutex poisoned while unwinding — use unwrap_or_else(PoisonError::into_inner) in both single_flight twins (authz single_flight.rs:391, engine twin).
Task 2: minor (deferred): busy-spin retry loop in tests/fragment.rs:623-635 — add thread::yield_now().
Task 2: note: RED evidence reconstructed compile-failure (implementation pre-existed in worktree, verified by implementer); weaker than behavioural RED, accepted.
Task 2: complete (commits d9baada..20e2893, spec clean — 1 important + 2 minors deferred with rulings)
Task 3: fix round 1/5 (2 addressed, 0 open — self-scaling test bounds; commits cbe7b04..d1f1cfc)
Task 3: minor (deferred): map_join_error lacks the mirror unit test map_store_error has (detail must not reach caller — I13).
Task 3: minor (deferred): needless Arc::clone of state in item/ingest/changes/authorise handlers (refcount-only, tidiness).
Task 3: note (semantics, reviewed as acceptable): an .await now sits between session lookup and engine call — a revoke accepted in that window no longer prevents the in-flight request completing; not a leak (M_auth materialised at authorise; suppressions unaffected). Recorded per reviewer request.
Task 3: note: x-tessera-server-us now includes blocking-pool queue delay; Task 4 moves its start after admission per D-E — comment there should note the composition change.
Task 3: note: /control/changes JSON body decode still on reactor via Json extractor (within brief's letter; ingest uses Bytes).
Task 3: complete (commits 20e2893..d1f1cfc, review clean after 1 fix round)
Task 4: minor (deferred): no bound check vs tokio Semaphore MAX_PERMITS — absurd compute_queue could wrap in release and yield a tiny slots semaphore; add refuse-style bound in config parse (state.rs:812/config.rs).
Task 4: minor (deferred): shed_total counts gate sheds only — ProjectionBuilding/FragmentBuilding 429s invisible; rename or document on the /control/status field (control.rs:394).
Task 4: minor (deferred): contracts §3.1 amendment embeds Rust identifiers + "D-B" into the wire doc — trim to endpoint list + Retry-After: 1 (contracts-spec:237).
Task 4: minor (deferred): available_parallelism() failure fallback to 1 degrades silently — add a startup log line.
Task 4: note (resolved ⚠): x-tessera-admission-us needs no contracts registration — timing headers are out of contract (contracts §6; confirmed in docs sweep).
Task 4: note (follow-up idea, not a defect): /session/authorise is now the likeliest cold-start 429 and emits no timing header.
Task 4: fix round 1/5 (1 addressed, 0 open — D-E comment corrected; commits fa497fe..f1e6ed1)
Task 4: complete (commits d1f1cfc..f1e6ed1, review clean after 1 fix round — 4 minors deferred)
Task 5: minor (deferred): server disconnect test doc — same checkpoint ambiguity as engine test; value is permit-release observation (http.rs:969) [being addressed in fix round 1 alongside the Important].
Task 5: minor (deferred): poll_until_in_flight 2s cap can turn a slow-runner regression into the helper panic instead of the ratio assertion (http.rs:1048/2249) — cosmetic, both are failures.
Task 5: fix round 1/5 (1 addressed, 0 open — test doc honesty + warm-up bias; commits e0a9e3b..af3d94c)
Task 5: complete (commits f1e6ed1..af3d94c, review clean after 1 fix round — 2 minors deferred)
Task 6: minor (deferred): I13 pool-panic test uses a look-alike pool, not Engine's own (session.rs:1071) — move onto a real Engine's pool if cheap.
Task 6: minor (deferred): byte-equality fixtures likely contain no empty tiles — Ok(None) skip unexercised by the headline tests; add a sparse/skewed bbox variant (tests/viewport.rs:2372, http.rs:2830).
Task 6: minor (deferred): EngineConfig compute_threads=0 falls back to rayon default for direct embedders (server refuses at parse) — documented asymmetry (session.rs:97-101).
Task 6: minor (deferred): TILE_PAR_MIN_LEN=4 argued not measured — candidate bench arm (viewport.rs:664).
Task 6: minor (deferred): no comment states the new cancellation bound (≤ compute_threads tiles in flight) at the sweep (viewport.rs:~635).
Task 6: minor (deferred): check-layers deny inspects normal deps depth 1 only — pre-existing script semantics.
Task 6: note: accepted deviation — explicit default_compute_threads() at 13 construction sites instead of Default impl (compile-enforced; judged better for security-relevant config).
Task 6: complete (commits af3d94c..ebfa1d4, review clean — 6 minors deferred)
Task 7: fix round 1/5 (2 addressed + 1 recorded, 0 open — true 2x peak, capacity hints; gate improved to 2.56x/2.62x; commits 8599f9a..7c310c4)
Task 7: note: gate baseline is new-code-at-1-thread per brief; vs pre-branch serial the gain may sit nearer 2x (recorded in report).
Task 7: complete (commits ebfa1d4..7c310c4, review clean after 1 fix round, gate PASSED and kept)
Task 8: complete (commits 7c310c4..cfa6e83, review clean — no issues)
Task 9: minor (deferred): run_shed_cell boot omits admission_timeout_ms while its hang_timeout reads the flag — latent desync if --admission-timeout-ms passed (bench_concurrency.py:~401/427).
Task 9: minor (deferred): pan-storm report over-claims "mechanism fired" — signals equally consistent with run-to-completion; mechanism proven by http.rs:2701 test instead; soften phrasing.
Task 9: minor (deferred): sample_cpu_mean reads samples before sampler.join(), deviating from the file's stop-join-read pattern (benign under GIL).
Task 9: ACCEPTANCE: criteria 2,3,5,6,7 PASS; criterion 1 PARTIAL FAIL (p99+CPU-signature pass; rps 10.1k vs ≥35k bar — bar predates the gate, structural; adjudication: criterion mis-derived + real small-scale latency regression); criterion 4 FAIL (79.99% vs 85% CPU — marginal/demand-side; c=1 threads=default 2.97x SLOWER than threads=1 — real, root shared with TILE_PAR_MIN_LEN uncalibrated deferral; follow-up candidate: serial fallback below work threshold). Full numbers in task-9-report.md.
Task 9: note: two shared-infra incidents documented (sibling-worktree 21GB test OOM-killing first matrix run; cross-worktree target cache staleness + disk 100%) — maintenance notes, not branch defects.
Task 9: complete (commits cfa6e83..561977c, review clean — 3 minors deferred; acceptance failures adjudicated to final summary)
Fix wave: complete (commits 561977c..af0aa67, 3 commits; 20 fixed / 1 already-satisfied / 4 skipped with sound reasons; re-review clean). Residual: clippy doc_lazy_continuation x5 in tessera-store/tests/permutation_project_parallel.rs:210-214 (introduced by 7c310c4) — folded into calibration task as rider.
Calibration task (user-directed, post-plan): two-axis calibration — (a) serial-fallback threshold for small viewports, (b) with_min_len chunk size (TILE_PAR_MIN_LEN=4 too small); plus bench metrics points/sec, points/user/sec, points/request; plus shed-cost interpretation in points. Bench outputs to persist under SDD workspace, not /tmp.
Calibration: fix round 1/5 (5 addressed, 0 open — dense-mask sweep confirms bands, constant kept + provenance; underlay demand in predictor; closure hoisted; const asserts; transcription; commits ee0e362..7e5b553)
Calibration: complete (commits af0aa67..7e5b553 incl. clippy rider 0b2554e; review clean after 1 fix round). Results: SERIAL_FALLBACK_MAX_ROWS=200_000 (Σ range.len() + underlay cells); TILE_PAR_MIN_LEN=8; c=1 small-viewport ratio 2.97x→1.01x; large-work 0.68x (parallel wins); Arm B c=100 9.1k→31.2k rps; points metrics added (Arm B c=100: 10.94M pts/s, 109.4k pts/user/s, 350 pts/req). Criterion 4b retro-passes; criterion 1 rps substantially recovered at c<=100.
Final whole-branch review: READY-WITH-FOLLOW-UPS (no Critical, no new Important; 3 minors → fixed in d03e46c, re-review clean). Accepted trade (documented in-code): /v1/items stale-epoch check now behind the admission gate (429-under-load possible before 409).
Merge follow-ups (surface to user): (1) unbounded in-memory caches — row projections (~100MB/session extrapolated at 1e9) + fragments (mmaps pinned) never evicted; needs argued bound/LRU + hooks on revoke/expiry + generation retirement BEFORE sustained-load or large-corpus deployment. (2) Calibration constants are one-box/one-corpus; re-calibrate at scale. (3) shed_total undercounts (gate sheds only). (4) Non-cancellable single-flight builds hold permits to completion (D-G design). (5) Small-scale rps acceptance criterion structurally mis-derived (adjudicated).
Branch complete at d03e46c (20 commits over 6052af2). Workspace deliberately NOT deleted: bench-runs/ raw outputs live here and are not in git.
Admission default retune (user-directed): compute_admission default = 4 x compute_threads (checked_mul, new overflow refusal + test); comments/tests updated; commit 1877546. Validated: c=100 shed 41-48% -> ~0, rps +13-18% (B c=100 35.4k, A c=1000 24.7k), p99 bounded (worst 93ms). c=5 unmoved — residual is the fixed spawn_blocking toll (recorded follow-up: inline-below-threshold fast path). Review: Approved, 1 minor (baseline provenance) — resolved by controller with git-show citation appended to admission-4x-report.md.
Branch final at 1877546 (21 commits over 6052af2). Full suite green at d03e46c + server suite green at 1877546.
Merge main (809c1b1: B9 tiered decode, 8-minute 1e9 pipeline, parallel digests) into branch: 2 conflicts resolved (timing.rs docs combined; both interleaved tests kept, main's adapted to 3-arg item + write_*_n helpers); 3 examples gained new BuildArgs fields. Full suite + bench-timing + layers green on merged tree (pipefail-verified). Byte-equality now covers B9 decode under parallel sweep.
MERGED TO MAIN: 53fa504 (merge of concurrency/viewpath at 3862a61) + follow-up fix (examples gain spilling-pipeline BuildArgs fields; main had moved again with d3581cb/49c5097). One conflict (authz lib.rs re-export union). Full workspace suite green on merged main, pipefail-verified. Branch + worktree retained: re-calibration continues there; its constants commit merges to main as a follow-up.
Re-calibration (§14, post-B9, three scales): commits 047e731 + 2c19e13 (merge main) + 99af9aa. SERIAL_FALLBACK_MAX_ROWS 200k -> 500M (natural viewports serial everywhere post-B9, 1.4-10x; parallel wins only full-extent whole-corpus sweeps, 0.28x at 1e9 = 3.6x faster; families overlap in row count so 500M is the safe compromise; corpus-size-aware knob noted as the complete fix, not landed unprompted). TILE_PAR_MIN_LEN=8 re-confirmed. Validation ratios 1.07/1.08/0.98. Review: spec clean; Important = parallel branch lost ALL test coverage (byte-equality tests vacuous below 500M) -> fix round 1 in flight (test-injectable threshold override); 2 minors folded (min_len doc overclaim; provenance nit).
PENDING (controller-owned, after fix round): production fix for StageTimings.rows_in_ranges mask-dependence (Task 6 regression: Ok(None) TileStats discarded -> counter undercounts; cited by C4 leak-register doc; telemetry-only, no authorisation decision reads it) — separate reviewed change, then merge branch -> main.
Re-calibration fix rounds: 1/5 (3c0f63c — test-injectable threshold override, per-Engine AtomicU64, bench-timing-gated setter; parallel-branch byte-equality coverage restored; min_len doc scoped; provenance fixed) + 2/5 (395bc11 — doc-only: comments now admit the always-present field + per-request relaxed load). Re-reviews clean. Re-calibration COMPLETE.
rows_in_ranges fix: e486bba (choice (a) serial-prefix count, exact pre-Task-6 semantics restored; reviewer reproduced RED at base in isolated worktree; Approved, no issues).
SECOND MERGE TO MAIN: 55eab5f (re-calibration 047e731..395bc11 + e486bba; no conflicts). Full workspace suite green on merged main. Workstream follow-ups remaining (unscheduled): cache eviction design (the one Important deferred with ruling); inline-below-threshold server fast path (c=5 toll); corpus-size-aware serial-fallback knob (noted in §14 as the complete fix over the 500M compromise); shed_total scope.
